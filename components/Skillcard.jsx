import React from 'react';
import '../CSS/Skillcard.css'
 
function skillCard({title}) {
    return (
        <>
            <pre> {title} </pre>
        </>
    );
}

export default skillCard
